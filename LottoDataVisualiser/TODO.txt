Implement better UI handling, best based on the DomainModels existing at the run time
- Add a loading spinner while data is being fetched for every tab for each game so tabs at least exist
- Implement QuickGraph, now I know it uses DataSet, but I have to find a way to convert my DomainModels to DataSet



done:
- Create Endpoint that will call for last draw for all games instead of calling once per game